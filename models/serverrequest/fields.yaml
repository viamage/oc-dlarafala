# ===================================
#  Form Field Definitions
# ===================================

fields:
    email:
        label: Email
    ram_amount:
        label: RAM
    cpu_amount:
        label: CPU
    vlan:
        label: VLAN
    ip_config:
        label: IP Config
